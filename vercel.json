{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "cleanUrls already maps /app to public/app.html and redirects /app.html to /app. Adding explicit rewrites on top of that is redundant and risks fighting the redirect, so they are deliberately absent.",
  "cleanUrls": true,
  "trailingSlash": false,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "X-Frame-Options", "value": "SAMEORIGIN" }
      ]
    }
  ]
}
